# Board package

This package will contain the behavior-preserving extraction of board-specific presentation and coordinate helpers from `ChessBoard.tsx`.

The first extraction should move only pure board helpers and board presentation. Game lifecycle, API calls, analysis state, and layout geometry stay outside this package.
